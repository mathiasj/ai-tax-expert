# Frontend environment variables
# API is proxied via Vite in development, no config needed.
# In production, set VITE_API_URL if the API is on a different origin.
# VITE_API_URL=https://api.example.com
